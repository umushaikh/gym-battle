// Stamped by the deploy workflow with the commit it built from and when.
// Left as placeholders for local/dev runs (node server.js without a deploy).
const APP_VERSION = { sha: '__BUILD_ID__', builtAt: '__BUILD_TIME__' };
